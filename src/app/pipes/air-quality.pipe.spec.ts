import { AirQualityPipe } from './air-quality.pipe';

describe('AirQualityPipe', () => {
  it('create an instance', () => {
    const pipe = new AirQualityPipe();
    expect(pipe).toBeTruthy();
  });
});
